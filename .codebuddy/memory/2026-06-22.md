# 2026-06-22 工作日志

## 项目背景
- Fast-Merge 是一个 VS Code 扩展（GitLab 合并请求管理），双层架构：扩展层（Node.js + esbuild）+ WebView 层（React 18 + Vite + Antd 5）。
- 根目录与 `webview-ui/` 各有独立 `package.json`，依赖分别安装。

## 今日完成
1. 修复 `webview-ui` 的 TS2875 错误（`react/jsx-runtime` 找不到）：原因是 `webview-ui/node_modules` 未安装，执行 `npm install` 后解决。
2. 解答项目同时存在 `package-lock.json` 与 `yarn.lock` 的疑问：根目录 `package-lock.json` 是旧项目（name="assistant"）遗留的过时文件，`yarn.lock` 才是当前有效锁文件。建议统一用 yarn 并删除多余 `package-lock.json`。
3. 创建 `CODEBUDDY.md`：基于 `CLAUDE.md`、`README.md` 及源码分析，整理出命令清单与双层架构（消息路由、postMessage 请求-响应协议、异步冲突检测、Cherry Pick 批量、配置 DES 加密存储、端口 25463、打包流程）说明。

## 注意事项
- 按 `CLAUDE.md` 约定，本项目使用 **yarn** 作为包管理器。
- 跨层通信消息类型定义在 `src/shared/WebviewMessage.ts`，新增消息需同步 `MyProvider.ts` 路由与 `useGitLabApi.ts`。
